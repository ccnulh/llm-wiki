flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
dashscope>=1.14.0
markdown>=3.5.0
python-frontmatter>=1.0.0

# Server
gunicorn>=21.0.0

# 文档格式支持
pypdf>=3.0.0
python-docx>=1.0.0
openpyxl>=3.1.0
xlrd>=2.0.1
python-pptx>=0.6.21
ebooklib>=0.18
mobi>=0.3.3
beautifulsoup4>=4.12.0
lxml>=4.9.0
Pillow>=10.0.0

# 网络请求 / 飞书
requests>=2.31.0
lark-oapi>=1.0.0

# 视频/播客抓取
yt-dlp>=2024.1.0

# 云存储
cos-python-sdk-v5>=1.9.42

# 注意：moviepy/pydub/whisper/easyocr/torch/pytesseract 等重依赖
# 在 Render free plan (512MB) 上构建/运行会 OOM，
# 所以不安装；importer 内部使用时 try/except 自动降级。
